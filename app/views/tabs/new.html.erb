<div class= 'header'>
  <div class= 'title'>
    <h1><%= link_to 'Taxonomista', projects_path %></h1> 
  </div> 

  <div class= "back-home">
    <%= link_to 'Voltar', project_path(@project) %>
  </div>
</div>


<div class= 'tab'>
  <%= form_for(@tab) do |t| %>
    <%= t.label "Digite o path do arquivo #{@project.name}/" %>
    <%= t.text_field :path %>
    <%= t.submit 'Cadastrar path' %>
  <% end %>

  <div class= 'tab-error'>
    <%= @tab.errors.full_messages.first if @tab.errors.any? %>
  </div>
</div>